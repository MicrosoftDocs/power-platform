---
title: Microsoft Power Platform CLI data command group| Microsoft Docs
description: "Describes commands and parameters for the Microsoft Power Platform CLI data command group."
keywords: "pac cli"
ms.subservice: developer
author: laneswenka
ms.author: laswenka
ms.reviewer: jdaly
ms.topic: generated-reference
contributors: JimDaly
no-loc: [ "pac", "pac data", "data bulk-delete cancel", "data bulk-delete list", "data bulk-delete pause", "data bulk-delete resume", "data bulk-delete schedule", "data bulk-delete show", "data export", "data import", "data retention enable-entity", "data retention list", "data retention set", "data retention show", "data retention status"]
---
<!-- 
Do not edit this file. 
This file is generated by a program and any changes will be overwritten when this topic is re-generated.
Use the include files to add additional content to this topic.
-->
# pac data

Import and export data from Dataverse.

[!INCLUDE [data-intro](includes/data-intro.md)]

## Commands

|Command|Description|
|---------|---------|
|[pac data bulk-delete cancel](#pac-data-bulk-delete-cancel)|Cancel a bulk delete job.|
|[pac data bulk-delete list](#pac-data-bulk-delete-list)|List bulk delete jobs in the environment.|
|[pac data bulk-delete pause](#pac-data-bulk-delete-pause)|Pause a running bulk delete job.|
|[pac data bulk-delete resume](#pac-data-bulk-delete-resume)|Resume a paused bulk delete job.|
|[pac data bulk-delete schedule](#pac-data-bulk-delete-schedule)|Schedule a bulk delete job for records in a table.|
|[pac data bulk-delete show](#pac-data-bulk-delete-show)|Show details of a specific bulk delete job.|
|[pac data export](#pac-data-export)|Export data|
|[pac data import](#pac-data-import)|Import data|
|[pac data retention enable-entity](#pac-data-retention-enable-entity)|Enable archival/retention for a specific table.|
|[pac data retention list](#pac-data-retention-list)|List retention policies configured in the environment.|
|[pac data retention set](#pac-data-retention-set)|Create or update a retention policy for a table.|
|[pac data retention show](#pac-data-retention-show)|Show details of a specific retention policy.|
|[pac data retention status](#pac-data-retention-status)|Check the status of a retention operation.|


## pac data bulk-delete cancel

Cancel a bulk delete job.

[!INCLUDE [data-bulk-delete-cancel-intro](includes/data-bulk-delete-cancel-intro.md)]


### Required Parameters for data bulk-delete cancel

#### `--id` `-id`

The bulk delete job ID (GUID).


### Optional Parameters for data bulk-delete cancel

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-bulk-delete-cancel-remarks](includes/data-bulk-delete-cancel-remarks.md)]

## pac data bulk-delete list

List bulk delete jobs in the environment.

[!INCLUDE [data-bulk-delete-list-intro](includes/data-bulk-delete-list-intro.md)]


### Optional Parameters for data bulk-delete list

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-bulk-delete-list-remarks](includes/data-bulk-delete-list-remarks.md)]

## pac data bulk-delete pause

Pause a running bulk delete job.

[!INCLUDE [data-bulk-delete-pause-intro](includes/data-bulk-delete-pause-intro.md)]


### Required Parameters for data bulk-delete pause

#### `--id` `-id`

The bulk delete job ID (GUID).


### Optional Parameters for data bulk-delete pause

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-bulk-delete-pause-remarks](includes/data-bulk-delete-pause-remarks.md)]

## pac data bulk-delete resume

Resume a paused bulk delete job.

[!INCLUDE [data-bulk-delete-resume-intro](includes/data-bulk-delete-resume-intro.md)]


### Required Parameters for data bulk-delete resume

#### `--id` `-id`

The bulk delete job ID (GUID).


### Optional Parameters for data bulk-delete resume

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-bulk-delete-resume-remarks](includes/data-bulk-delete-resume-remarks.md)]

## pac data bulk-delete schedule

Schedule a bulk delete job for records in a table.

[!INCLUDE [data-bulk-delete-schedule-intro](includes/data-bulk-delete-schedule-intro.md)]


### Required Parameters for data bulk-delete schedule

#### `--entity` `-e`

The logical name of the table (e.g., account, activitypointer).


### Optional Parameters for data bulk-delete schedule

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

#### `--fetchxml` `-fx`

FetchXML query to filter records. If omitted, all records in the table are targeted.

#### `--job-name` `-jn`

A descriptive name for the bulk delete job.

#### `--recurrence` `-r`

Recurrence pattern for the job (e.g., FREQ=DAILY;INTERVAL=1).

#### `--start-time` `-st`

The scheduled start time in ISO 8601 format (e.g., 2025-06-01T00:00:00Z). Defaults to now.

[!INCLUDE [data-bulk-delete-schedule-remarks](includes/data-bulk-delete-schedule-remarks.md)]

## pac data bulk-delete show

Show details of a specific bulk delete job.

[!INCLUDE [data-bulk-delete-show-intro](includes/data-bulk-delete-show-intro.md)]


### Required Parameters for data bulk-delete show

#### `--id` `-id`

The bulk delete job ID (GUID).


### Optional Parameters for data bulk-delete show

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-bulk-delete-show-remarks](includes/data-bulk-delete-show-remarks.md)]

## pac data export

Export data

[!INCLUDE [data-export-intro](includes/data-export-intro.md)]


### Required Parameters for data export

#### `--schemaFile` `-sf`

Schema file name. It can be created using the Configuration Migration Tool.


### Optional Parameters for data export

#### `--dataFile` `-df`

File name for data zip file. Default data.zip

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

#### `--overwrite` `-o`

Allow overwrite of the output data file if it already exists.

This parameter requires no value. It's a switch.

#### `--verbose` `-v`

Output more diagnostic information during data import/export

This parameter requires no value. It's a switch.

[!INCLUDE [data-export-remarks](includes/data-export-remarks.md)]

## pac data import

Import data

[!INCLUDE [data-import-intro](includes/data-import-intro.md)]


### Required Parameters for data import

#### `--data` `-d`

Zip file or directory name with data for import.


### Optional Parameters for data import

#### `--connection-count` `-c`

Number of parallel connections to use during import. Default: 5

#### `--dataDirectory` `-dd`

**Deprecated**: This parameter is ignored.
#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

#### `--verbose` `-v`

Output more diagnostic information during data import/export

This parameter requires no value. It's a switch.

[!INCLUDE [data-import-remarks](includes/data-import-remarks.md)]

## pac data retention enable-entity

Enable archival/retention for a specific table.

[!INCLUDE [data-retention-enable-entity-intro](includes/data-retention-enable-entity-intro.md)]


### Required Parameters for data retention enable-entity

#### `--entity` `-e`

The logical name of the table to configure retention for.


### Optional Parameters for data retention enable-entity

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-retention-enable-entity-remarks](includes/data-retention-enable-entity-remarks.md)]

## pac data retention list

List retention policies configured in the environment.

[!INCLUDE [data-retention-list-intro](includes/data-retention-list-intro.md)]


### Optional Parameters for data retention list

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-retention-list-remarks](includes/data-retention-list-remarks.md)]

## pac data retention set

Create or update a retention policy for a table.

[!INCLUDE [data-retention-set-intro](includes/data-retention-set-intro.md)]


### Required Parameters for data retention set

#### `--criteria` `-c`

FetchXML criteria defining which records to retain.

#### `--entity` `-e`

The logical name of the table to configure retention for.


### Optional Parameters for data retention set

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

#### `--recurrence` `-r`

Recurrence pattern for the job (e.g., FREQ=DAILY;INTERVAL=1).

#### `--start-time` `-st`

The scheduled start time in ISO 8601 format (e.g., 2025-06-01T00:00:00Z). Defaults to now.

[!INCLUDE [data-retention-set-remarks](includes/data-retention-set-remarks.md)]

## pac data retention show

Show details of a specific retention policy.

[!INCLUDE [data-retention-show-intro](includes/data-retention-show-intro.md)]


### Required Parameters for data retention show

#### `--id` `-id`

The retention configuration ID (GUID).


### Optional Parameters for data retention show

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-retention-show-remarks](includes/data-retention-show-remarks.md)]

## pac data retention status

Check the status of a retention operation.

[!INCLUDE [data-retention-status-intro](includes/data-retention-status-intro.md)]


### Required Parameters for data retention status

#### `--id` `-id`

The retention operation ID (GUID).


### Optional Parameters for data retention status

#### `--environment` `-env`

Specifies the target Dataverse. The value may be a Guid or absolute https URL. When not specified, the active organization selected for the current auth profile will be used.

[!INCLUDE [data-retention-status-remarks](includes/data-retention-status-remarks.md)]

[!INCLUDE [data-remarks](includes/data-remarks.md)]

### See also

[Microsoft Power Platform CLI Command Groups](index.md)<br />
[Microsoft Power Platform CLI overview](../introduction.md)